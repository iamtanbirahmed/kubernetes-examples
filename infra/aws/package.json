{
  "name": "@kubernetes-examples/aws",
  "version": "0.1.3",
  "bin": {
    "aws": "bin/main.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run build",
          "cwd": "infra/aws"
        }
      },
      "lint": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run cdk doctor",
          "cwd": "infra/aws"
        }
      },
      "deploy": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run cdk deploy -- --context ENV={args.envName} --require-approval=never --all",
          "cwd": "infra/aws"
        },
        "configurations": {
          "staging": {
            "args": [
              "--envName=staging"
            ]
          }
        }
      },
      "test": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run test",
          "cwd": "infra/aws"
        }
      },
      "destroy": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run cdk destroy -- --context ENV={args.envName} --all --force",
          "cwd": "infra/aws"
        },
        "configurations": {
          "staging": {
            "args": [
              "--envName=staging"
            ]
          }
        }
      },
      "diff": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run cdk diff -- --context ENV={args.envName}",
          "cwd": "infra/aws"
        },
        "configurations": {
          "staging": {
            "args": [
              "--envName=staging"
            ]
          }
        }
      },
      "bootstrap": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run cdk bootstrap",
          "cwd": "infra/aws"
        }
      },
      "synth": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npm run cdk synth -- --context ENV={args.envName}",
          "cwd": "infra/aws"
        },
        "configurations": {
          "staging": {
            "args": [
              "--envName=staging"
            ]
          }
        }
      }
    }
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1033.0",
    "cdk-nag": "^2.37.55",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@aws-cdk/lambda-layer-kubectl-v31": "^2.1.0",
    "@aws-cdk/lambda-layer-kubectl-v34": "^2.0.0",
    "aws-cdk-lib": "2.215.0",
    "constructs": "^10.0.0"
  }
}
